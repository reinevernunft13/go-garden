package main

// in func(receptors)
func (app *Config) makeUI() {
	//obtains API data (rain probability, temp max & min, humidity)

	//inserts info into container and the container

	//adds the container to the window

}
